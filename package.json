{
  "name": "hyper-plugin-extend",
  "version": "1.1.0",
  "description": "Allows hyper plugins to integrate with each other.",
  "main": "dist/index.js",
  "scripts": {
    "lint": "tslint ./src/**.ts",
    "prebuild": "npm run lint && rimraf ./dist",
    "build": "tsc -d ./src/**.ts --outdir dist",
    "prepublishonly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/OrionNebula/hyper-plugin-extend"
  },
  "keywords": [
    "hyper",
    "hyperterm"
  ],
  "author": "Olivia Trewin",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.6.2",
    "tsc": "^1.20150623.0",
    "tslint": "^5.10.0",
    "tslint-config-standard": "^7.0.0",
    "typescript": "^2.8.3"
  }
}
